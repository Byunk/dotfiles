# CLAUDE.md

## Language Preferences

### Python

- Always use `uv` instead of `python3` or `pip3`
- Always use `ruff` instead of `flake8` or `pylint`

### JavaScript or TypeScript

- Always use `pnpm` instead of `npm` or `yarn`
